# BFF — 미설정이면 인메모리 어댑터(외부 의존성 0)로 뜬다
# DATABASE_URL=postgres://tooday:tooday@localhost:5432/tooday
# REDIS_URL=redis://localhost:6379
# BFF_JWT_SECRET=change-me           # 프로덕션 필수
# BFF_PORT=3002
# BFF_ALLOWED_ORIGINS=http://localhost:8080

# 테스트 — SQL 어댑터 계약 테스트는 이 값이 있을 때만 돈다
# TOODAY_TEST_DATABASE_URL=postgres://postgres@127.0.0.1:5432/postgres
