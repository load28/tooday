pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{ts,tsx,json}"
      run: bunx biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true
    - name: deps
      glob: "*.{ts,tsx}"
      run: bun run lint:deps
