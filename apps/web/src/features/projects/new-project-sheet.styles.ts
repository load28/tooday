import * as stylex from '@stylexjs/stylex';
import { space } from '@/styles/tokens.stylex';

export const styles = stylex.create({
  colorRow: { paddingBlock: space['2xs'], paddingInline: space['2xs'] },
});
